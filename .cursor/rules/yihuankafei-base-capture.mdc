---
description: 异环脚本坐标基准与16:9归一化提醒
alwaysApply: true
---

# 异环脚本基准规则

## 1) 坐标与窗口基准（所有脚本必须遵守）

- 目标程序固定为：`异环`
- 目标分辨率基准固定为：`1600*900`
- 坐标原点固定为：窗口客户端左上角（不含标题栏）`(0,0)`
- 所有涉及坐标点击、取色、截图区域、模板匹配的脚本，均以该基准表达与记录坐标

## 2) 图像与图色相关脚本的归一化提醒

- 当任务涉及图片、图色、截图模板、像素坐标运算时，必须先提醒用户：
  - 是否需要做坐标/区域归一化处理，以适配所有 `16:9` 分辨率
- 若用户确认需要归一化：
  - 以 `1600x900` 作为设计基准
  - 将坐标与区域按比例换算到目标 `16:9` 分辨率再执行
- 若用户明确不需要归一化：
  - 保持 `1600x900` 固定坐标逻辑，并在说明中注明仅适配该分辨率
